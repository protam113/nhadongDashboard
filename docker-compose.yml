
services:
  nhadongdashboard:
    container_name: dashboard
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: unless-stopped

networks:
  default:
    driver: bridge